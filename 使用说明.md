# 📝 作业小管家

帮小朋友按科目记录每天的家庭作业，再也不怕忘记啦！

## ✨ 功能

- **📝 作业记录**：按科目（语文/数学/英语…）添加作业，可写备注、设置截止日期；完成后打勾，还会撒彩带庆祝 🎉
- **📅 逾期提醒**：过了截止日期还没完成的作业会自动标红，显示"已逾期 N 天"
- **🌟 打卡统计**：每天完成作业后点"打卡"，统计今日/本周完成情况、最近 7 天图表、连续打卡天数
- **⏰ 番茄钟**：内置 25 分钟专注计时器，写完一段休息一下
- **⚙️ 设置**：科目可增删、改顶栏称呼、导出/导入备份、清空已完成、全部重置
- **📴 离线可用**：安装到桌面后，没网也能用，数据都存在手机本地

## 🚀 怎么用

### 方式一：最快（电脑上直接看）
双击打开 `index.html` 就能用，不需要装任何东西。

### 方式二：让手机也用（推荐，支持安装到桌面）

1. 安装 [Node.js](https://nodejs.org)（或 Python 也可以）
2. 双击运行 `启动服务.ps1`（或在终端执行 `node server.js`）
3. 电脑和手机连同一个 WiFi，手机上用浏览器打开屏幕上显示的地址，例如 `http://192.168.1.5:8080`
4. 在手机浏览器里把它"添加到主屏幕"，就像装了真正的 app：
   - **安卓（Chrome）**：浏览器菜单 ⋮ →「添加到主屏幕」
   - **iPhone（Safari）**：分享按钮 →「添加到主屏幕」

> 想在公网上长期访问？可以免费部署到 [GitHub Pages](https://pages.github.com/) 或 [Vercel](https://vercel.com)，把整个文件夹传上去即可。

## 🗂️ 文件说明

| 文件 | 作用 |
| --- | --- |
| `index.html` | 页面入口 |
| `css/style.css` | 样式 |
| `js/app.js` | 全部逻辑（作业、统计、番茄钟） |
| `manifest.webmanifest` / `sw.js` | PWA 配置 + 离线缓存 |
| `icons/` | App 图标 |
| `server.js` / `启动服务.ps1` | 本地起服务，让手机访问 |

## 💾 数据安全

- 所有数据保存在**手机浏览器的本地存储**里，换手机/清浏览器缓存会丢失
- 建议定期在「设置 → 导出备份」下载一份 JSON 文件，以后用「导入备份」就能恢复

## 🛠️ 技术

纯原生 HTML/CSS/JS，无任何依赖，不用打包。数据模型：`localStorage` 存作业列表 + 打卡记录。
